# InventoryPro Docker Environment Configuration
# Copy this file to .env.docker and update the values for your deployment

# Database Configuration
POSTGRES_DB=inventorypro
POSTGRES_USER=postgres
POSTGRES_PASSWORD=SecurePassword2025!
POSTGRES_PORT=5432

# Application Configuration
APP_PORT=5000
NODE_ENV=production

# Security Configuration
SESSION_SECRET=your_very_long_random_session_secret_key_here_change_in_production
JWT_SECRET=your_very_long_random_jwt_secret_key_here_change_in_production
JWT_EXPIRES_IN=7d

# Email Configuration (Required for volunteer system receipt emails)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-specific-password
# SMTP_FROM_NAME=InventoryPro
# ADMIN_EMAIL=admin@yourcompany.com

# Logo Library & Media Storage
# File uploads are stored locally in /app/uploads directory
# Logo library supports: PNG, JPG, JPEG, GIF, SVG
# Maximum file size: 5MB per file
# Files persist in ./uploads/ host directory via Docker volume mount

# Volunteer System Features
# - Email-only authentication with 24-hour sessions
# - Mobile-optimized QR code scanning interface
# - Automated receipt emails for volunteer sales
# - Limited permissions (sales and inventory view only)
# - No additional configuration required